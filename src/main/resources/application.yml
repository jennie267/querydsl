spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/querydsl
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create    # application ??? ??? drop ? ?? ??
    properties:
      hibernate:
#        show_sql: true     ?? ??? logger? ??? ? ?? (????? ???? ????)
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace